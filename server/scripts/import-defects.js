const fs=require('fs'),path=require('path'),db=require('../db'),{matchTk,logUnmatched}=require('../lib/matchTk');const d=JSON.parse(fs.readFileSync(path.join(__dirname,'../data/DEFECTS_KTEK.json'),'utf8'));db.exec("DELETE FROM defects;DELETE FROM unmatched_tk WHERE entity_type='defect'");const q=db.prepare('INSERT INTO defects VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)');db.transaction(()=>d.forEach((x,i)=>{const id=String(x.id||i+1),m=matchTk(x.tk);q.run(id,x.source||'',x.dateObserved||'',x.tk||'',m.nodeId,x.address||'',x.defectType||'',x.networkType||'',x.note||'',x.detectedBy||'',x.priority||0,x.planDate||'',+!!x.resolved,x.resolveDate||'',x.master||'',JSON.stringify(x.photos||[]),JSON.stringify(x));logUnmatched('defect',id,x.tk,m)}))();console.log(JSON.stringify({defects:d.length}));
